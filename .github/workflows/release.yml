name: Release Desktop App

on:
  repository_dispatch:
    types: [release]
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Version bump type'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
      platforms:
        description: 'Platforms to build'
        required: true
        type: choice
        options:
          - all
          - macos-only
          - linux-only
          - windows-only
        default: all
      dry_run:
        description: 'Test mode - skips push to main, creates test release'
        required: false
        type: boolean
        default: false

env:
  NODE_VERSION: '22.22.2'

permissions:
  contents: write
  pull-requests: write

# Prevent concurrent releases - only one release at a time
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  version-bump:
    name: Bump Version
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.bump.outputs.new_version }}
      platforms: ${{ steps.inputs.outputs.platforms }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false

      - name: Determine bump type
        id: inputs
        run: |
          # Support both repository_dispatch and workflow_dispatch
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            BUMP_TYPE="${{ github.event.client_payload.bump_type }}"
            DRY_RUN="false"
            PLATFORMS="all"
          else
            BUMP_TYPE="${{ inputs.bump_type }}"
            DRY_RUN="${{ inputs.dry_run }}"
            PLATFORMS="${{ inputs.platforms }}"
          fi

          echo "bump_type=$BUMP_TYPE" >> $GITHUB_OUTPUT
          echo "dry_run=$DRY_RUN" >> $GITHUB_OUTPUT
          echo "platforms=$PLATFORMS" >> $GITHUB_OUTPUT
          echo "Bump type: $BUMP_TYPE, Dry run: $DRY_RUN, Platforms: $PLATFORMS"

      - name: Calculate new version
        id: bump
        run: |
          # Read current version from package.json
          CURRENT_VERSION=$(node -p "require('./apps/desktop/package.json').version")
          echo "Current version: $CURRENT_VERSION"

          # Parse version components
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"

          # Bump based on type
          case "${{ steps.inputs.outputs.bump_type }}" in
            major)
              NEW_VERSION="$((MAJOR + 1)).0.0"
              ;;
            minor)
              NEW_VERSION="${MAJOR}.$((MINOR + 1)).0"
              ;;
            patch)
              NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))"
              ;;
            *)
              echo "Error: Invalid bump_type: ${{ steps.inputs.outputs.bump_type }}"
              exit 1
              ;;
          esac

          echo "New version: $NEW_VERSION"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

      - name: Update package.json version
        run: |
          cd apps/desktop
          npm version ${{ steps.bump.outputs.new_version }} --no-git-tag-version

      - name: Validate no previous release artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.new_version }}"
          echo "=== Validating no stale artifacts exist for v${VERSION} ==="

          CONFLICTS=""

          # Check for existing tag
          if git ls-remote --tags origin | grep -q "refs/tags/v${VERSION}$"; then
            CONFLICTS="${CONFLICTS}\n  - Tag: v${VERSION} (delete with: git push origin --delete v${VERSION})"
          fi

          # Check for existing branch
          if git ls-remote --heads origin | grep -q "refs/heads/release/v${VERSION}$"; then
            CONFLICTS="${CONFLICTS}\n  - Branch: release/v${VERSION} (delete with: git push origin --delete release/v${VERSION})"
          fi

          # Check for existing GitHub release
          if gh release view "v${VERSION}" &>/dev/null; then
            CONFLICTS="${CONFLICTS}\n  - GitHub Release: v${VERSION} (delete with: gh release delete v${VERSION} --yes --cleanup-tag)"
          fi

          if [ -n "$CONFLICTS" ]; then
            echo ""
            echo "============================================"
            echo "ERROR: Found existing release artifacts for v${VERSION}"
            echo "============================================"
            echo ""
            echo "The following resources must be manually deleted before re-running:"
            echo -e "$CONFLICTS"
            echo ""
            echo "This usually happens when a previous release attempt or dry run"
            echo "partially completed. Clean up the resources above and re-run."
            echo "============================================"
            exit 1
          fi

          echo "No conflicts found, safe to proceed"

      - name: Create release PR and merge
        if: steps.inputs.outputs.dry_run != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.new_version }}"
          BRANCH="release/v${VERSION}"

          # Check if version bump is already on main (PR merged in a previous run)
          git fetch origin main
          MAIN_VERSION=$(git show origin/main:apps/desktop/package.json | node -p "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')).version")
          if [ "$MAIN_VERSION" == "$VERSION" ]; then
            echo "Version ${VERSION} already on main — PR was merged in a previous run, skipping"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create release branch
          git checkout -b "$BRANCH"
          git add apps/desktop/package.json
          git commit -m "chore(release): bump version to ${VERSION}"
          git push origin "$BRANCH"

          # Create PR
          PR_URL=$(gh pr create \
            --title "chore(release): bump version to ${VERSION}" \
            --body "Automated release PR for v${VERSION}" \
            --base main \
            --head "$BRANCH")
          echo "Created PR: $PR_URL"

          # PR will be approved by Cloudflare Worker (private infrastructure)
          # Enable auto-merge (squash)
          gh pr merge "$BRANCH" --squash --auto --delete-branch

          # Wait for PR to be merged (max 3 minutes)
          echo "Waiting for auto-merge..."
          for i in {1..36}; do
            sleep 5
            STATE=$(gh pr view "$BRANCH" --json state --jq '.state')
            echo "PR state: $STATE"
            if [ "$STATE" == "MERGED" ]; then
              echo "PR merged successfully"
              break
            fi
            if [ "$STATE" == "CLOSED" ]; then
              echo "Error: PR was closed without merging"
              exit 1
            fi
          done

          # Verify merge happened
          FINAL_STATE=$(gh pr view "$BRANCH" --json state --jq '.state')
          if [ "$FINAL_STATE" != "MERGED" ]; then
            echo "Error: PR did not merge within timeout"
            exit 1
          fi

      - name: Create and push release tag
        run: |
          VERSION="${{ steps.bump.outputs.new_version }}"
          DRY_RUN="${{ steps.inputs.outputs.dry_run }}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ "$DRY_RUN" == "true" ]; then
            echo "DRY RUN: Creating test tag without PR"
            git tag "v${VERSION}"
            git push origin "v${VERSION}" --force
          else
            git fetch origin main
            git checkout main
            git pull origin main
            git tag "v${VERSION}"
            git push origin "v${VERSION}"
          fi

  build-mac-arm64:
    name: Build macOS (Apple Silicon)
    needs: version-bump
    if: needs.version-bump.outputs.platforms == 'all' || needs.version-bump.outputs.platforms == 'macos-only'
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Node.js binaries
        run: pnpm -F @accomplish/desktop download:nodejs

      - name: Build desktop app
        run: pnpm -F @accomplish/desktop build

      - name: Package (unsigned)
        run: node scripts/package.cjs --mac --arm64 --publish never
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHON: python
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Accomplish-macOS-arm64
          path: |
            apps/desktop/release/*-arm64.dmg
            apps/desktop/release/*-arm64.zip
          retention-days: 30

  build-mac-x64:
    name: Build macOS (Intel)
    needs: version-bump
    if: needs.version-bump.outputs.platforms == 'all' || needs.version-bump.outputs.platforms == 'macos-only'
    runs-on: macos-15-large
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Node.js binaries
        run: pnpm -F @accomplish/desktop download:nodejs

      - name: Build desktop app
        run: pnpm -F @accomplish/desktop build

      - name: Package (unsigned)
        run: node scripts/package.cjs --mac --x64 --publish never
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYTHON: python
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Accomplish-macOS-x64
          path: |
            apps/desktop/release/*-x64.dmg
            apps/desktop/release/*-x64.zip
          retention-days: 30

  build-linux-arm64:
    name: Build Linux (ARM64)
    needs: version-bump
    if: needs.version-bump.outputs.platforms == 'all' || needs.version-bump.outputs.platforms == 'linux-only'
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libnotify-dev libnss3 libxss1 libxtst6 xvfb ruby ruby-dev rubygems build-essential
          sudo gem install --no-document fpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Node.js binaries
        run: pnpm -F @accomplish/desktop download:nodejs

      - name: Build desktop app
        run: pnpm -F @accomplish/desktop build

      - name: Package (unsigned)
        run: node scripts/package.cjs --linux --arm64 --publish never
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USE_SYSTEM_FPM: 'true'

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Accomplish-Linux-arm64
          path: |
            apps/desktop/release/*-arm64.AppImage
            apps/desktop/release/*-arm64.deb
          retention-days: 30

  build-linux-x64:
    name: Build Linux (x64)
    needs: version-bump
    if: needs.version-bump.outputs.platforms == 'all' || needs.version-bump.outputs.platforms == 'linux-only'
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libnotify-dev libnss3 libxss1 libxtst6 xvfb

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Node.js binaries
        run: pnpm -F @accomplish/desktop download:nodejs

      - name: Build desktop app
        run: pnpm -F @accomplish/desktop build

      - name: Package (unsigned)
        run: node scripts/package.cjs --linux --x64 --publish never
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Accomplish-Linux-x64
          path: |
            apps/desktop/release/*-x64.AppImage
            apps/desktop/release/*-x64.deb
          retention-days: 30

  build-windows-x64:
    name: Build Windows (x64)
    needs: version-bump
    if: needs.version-bump.outputs.platforms == 'all' || needs.version-bump.outputs.platforms == 'windows-only'
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Download Node.js binaries
        run: pnpm -F @accomplish/desktop download:nodejs --platform=win32-x64

      - name: Build desktop app
        run: pnpm -F @accomplish/desktop build

      - name: Package (unsigned)
        run: node scripts/package.cjs --win --x64 --publish never
        working-directory: apps/desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: Accomplish-Windows-x64
          path: apps/desktop/release/*.exe
          retention-days: 30

  create-release:
    name: Create GitHub Release
    needs:
      [
        version-bump,
        build-mac-arm64,
        build-mac-x64,
        build-linux-arm64,
        build-linux-x64,
        build-windows-x64,
      ]
    if: always() && needs.version-bump.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Check build results
        run: |
          PLATFORMS="${{ needs.version-bump.outputs.platforms }}"
          MAC_ARM64="${{ needs.build-mac-arm64.result }}"
          MAC_X64="${{ needs.build-mac-x64.result }}"
          LINUX_ARM64="${{ needs.build-linux-arm64.result }}"
          LINUX_X64="${{ needs.build-linux-x64.result }}"
          WIN_X64="${{ needs.build-windows-x64.result }}"

          echo "Requested platforms: $PLATFORMS"
          echo "Build results: macOS-arm64=$MAC_ARM64, macOS-x64=$MAC_X64, Linux-arm64=$LINUX_ARM64, Linux-x64=$LINUX_X64, Windows-x64=$WIN_X64"

          # Validate all requested platform builds succeeded
          FAILED=0

          if [ "$PLATFORMS" == "all" ] || [ "$PLATFORMS" == "macos-only" ]; then
            if [ "$MAC_ARM64" != "success" ]; then
              echo "Error: macOS ARM64 build failed (result: $MAC_ARM64)"
              FAILED=1
            fi
            if [ "$MAC_X64" != "success" ]; then
              echo "Error: macOS x64 build failed (result: $MAC_X64)"
              FAILED=1
            fi
          fi

          if [ "$PLATFORMS" == "all" ] || [ "$PLATFORMS" == "linux-only" ]; then
            if [ "$LINUX_ARM64" != "success" ]; then
              echo "Error: Linux ARM64 build failed (result: $LINUX_ARM64)"
              FAILED=1
            fi
            if [ "$LINUX_X64" != "success" ]; then
              echo "Error: Linux x64 build failed (result: $LINUX_X64)"
              FAILED=1
            fi
          fi

          if [ "$PLATFORMS" == "all" ] || [ "$PLATFORMS" == "windows-only" ]; then
            if [ "$WIN_X64" != "success" ]; then
              echo "Error: Windows x64 build failed (result: $WIN_X64)"
              FAILED=1
            fi
          fi

          if [ $FAILED -eq 1 ]; then
            echo "Error: One or more requested platform builds failed"
            exit 1
          fi

          echo "All requested platform builds succeeded"

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: v${{ needs.version-bump.outputs.new_version }}

      - name: Determine if dry run
        id: check
        run: |
          if [ "${{ github.event_name }}" == "repository_dispatch" ]; then
            echo "dry_run=false" >> $GITHUB_OUTPUT
          else
            echo "dry_run=${{ inputs.dry_run }}" >> $GITHUB_OUTPUT
          fi

      - name: Download macOS ARM64 artifacts
        if: needs.build-mac-arm64.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: Accomplish-macOS-arm64
          path: ./release/arm64

      - name: Download macOS x64 artifacts
        if: needs.build-mac-x64.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: Accomplish-macOS-x64
          path: ./release/x64

      - name: Download Windows x64 artifacts
        if: needs.build-windows-x64.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: Accomplish-Windows-x64
          path: ./release/win-x64

      - name: Download Linux ARM64 artifacts
        if: needs.build-linux-arm64.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: Accomplish-Linux-arm64
          path: ./release/linux-arm64

      - name: Download Linux x64 artifacts
        if: needs.build-linux-x64.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: Accomplish-Linux-x64
          path: ./release/linux-x64

      - name: List release artifacts
        run: |
          echo "Release artifacts:"
          find ./release -type f 2>/dev/null || echo "No artifacts found"

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.version-bump.outputs.new_version }}
          name: ${{ steps.check.outputs.dry_run == 'true' && '[TEST] ' || '' }}Accomplish v${{ needs.version-bump.outputs.new_version }}
          draft: false
          prerelease: ${{ steps.check.outputs.dry_run == 'true' }}
          generate_release_notes: true
          files: |
            ./release/arm64/*
            ./release/x64/*
            ./release/win-x64/*
            ./release/linux-arm64/*
            ./release/linux-x64/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify Slack of release
        if: success() && steps.check.outputs.dry_run != 'true'
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_RELEASE_WEBHOOK_URL }}
        run: |
          VERSION="${{ needs.version-bump.outputs.new_version }}"
          RELEASE_URL="https://github.com/${{ github.repository }}/releases/tag/v${VERSION}"

          # Build Slack message payload
          PAYLOAD=$(jq -n \
            --arg version "$VERSION" \
            --arg url "$RELEASE_URL" \
            '{
              blocks: [
                {
                  type: "section",
                  text: {
                    type: "mrkdwn",
                    text: "✅ *Accomplish v\($version) Released!*\n\nDownload the latest version from GitHub."
                  },
                  accessory: {
                    type: "button",
                    text: { type: "plain_text", text: "View Release", emoji: true },
                    url: $url,
                    style: "primary"
                  }
                }
              ]
            }')

          # Send to Slack (skip if webhook not configured)
          if [ -n "$SLACK_WEBHOOK" ]; then
            curl -f -X POST -H 'Content-type: application/json' \
              --data "$PAYLOAD" \
              "$SLACK_WEBHOOK" || echo "Warning: Failed to send Slack notification"
          else
            echo "SLACK_RELEASE_WEBHOOK_URL not configured, skipping notification"
          fi
