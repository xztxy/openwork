# Build-time config — injected by CI for official Free releases.
# Community/OSS builds don't need this file. All features degrade gracefully when absent.
#
# To test locally, copy this file to build.env and fill in values:
#   cp build.env.template build.env
#
BUILD_ENV_VERSION=1
MIXPANEL_TOKEN=
GA_API_SECRET=
GA_MEASUREMENT_ID=
SENTRY_DSN=
ACCOMPLISH_GATEWAY_URL=
